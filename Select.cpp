#include "Select.h"
#include <iostream>

Select::Select(const string& imeT, const string& kol, const string& vred): imeTabele(imeT), whereKolona(kol), whereVr(vred) {}

void Select::izvrsi(BazaPodataka& bp)
{
	cout << endl;
	//ispisi select
}
